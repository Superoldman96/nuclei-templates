id: wix-detect

info:
  name: Wix Detection
  author: Chirag Mistry
  severity: info
  description: |
    An instance running Wix is detected by looking for specific HTTP headers, HTML tags, and known endpoints.
  metadata:
    verified: true
    max-request: 8
  tags: wix,detect,tech

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/robots.txt"
      - "{{BaseURL}}/sitemap.xml"
      - "{{BaseURL}}/favicon.ico"
      - "{{BaseURL}}/_static/"
      - "{{BaseURL}}/wix-api"
      - "{{BaseURL}}/user/login"
      - "{{BaseURL}}/wix.js"

    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: word
        part: response
        words:
          - "Powered by Wix"
          - "X-Wix-Request-Id"
          - "Wix Static Site"
          - "wixsite.com"
        condition: or
        case-insensitive: true

      - type: status
        status:
          - 200
          - 403
          - 404
        condition: or

    extractors:
      - type: regex
        part: body
        name: version
        regex:
          - 'Wix.com ([0-9.]+)'
          - 'Wix version ([0-9.]+)'
          - 'Wix Static Site Version ([0-9.]+)'

      - type: regex
        part: body
        name: wix_id
        regex:
          - 'wix-id=([a-z0-9]+)'
          - 'siteid="([a-zA-Z0-9]+)"'
# digest: 490a004630440220018b5caef99e5630f4dd6a8302b2d7625cffcc5624eeefd657570be37cd55fe802202c5ce49ad3937429b6cd9a166b2a5660dea2b68440005d1bae8206dbc286f5cd:922c64590222798bb761d5b6d8e72950